- Currently, a logged in user can remove the profile in the url leading to 'blank' viewedProfileUsername, must fix so that whenever a user is logged in they cant to this and everytime they enter the show page they see the [username] path only

- Create backup pages for when invalid urls are made, eg profile/sgsdgfg/

- lock scrolling when modal shows up